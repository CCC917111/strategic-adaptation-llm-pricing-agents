# Never commit a populated .env file.
GEMINI_API_KEY=replace_with_your_key
# Select a model with the runner's --model option.
# The runner reads environment variables; it does not load .env automatically.
